#include "../../minishell.h"

void	init_arenas(t_minishell *mini)
{
	t_arena	strings;
	t_arena tokens;
	t_arena 

	arena_init()
}

token = get_arena_element_start(&tokens, sizeof(t_token_iteri);

token.token_string = get_arena_element_start(&strings)
if (!grow_arena_element(&string, 1))
{
	arena_str_cpy(&strings, 1);
}
	//error ;